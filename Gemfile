source "https://rubygems.org"

# Локальный просмотр: bundle install && bundle exec jekyll serve
# На GitHub Pages сборкой занимается actions/jekyll-build-pages, Gemfile там не нужен.
gem "jekyll", "~> 4.3"
